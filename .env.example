# Copy to .env and fill in. NEVER commit .env (gitignored). Use a throwaway testnet burner key.
PRIVATE_KEY=0xyour_testnet_burner_private_key
RPC_URL=https://api.infra.testnet.somnia.network/

# Somnia agent platform (testnet). See prompt.md / docs for mainnet.
PLATFORM_ADDRESS=0x037Bb9C718F3f7fe5eCBDB0b600D607b52706776

# Agent ids. JSON API is the prompt's canonical id; confirm the LLM id from the testnet docs/explorer.
JSON_API_AGENT_ID=13174292974160097713
LLM_AGENT_ID=

# Deployed singleton (filled in after Stage B / Covenant deploy)
COVENANT_ADDRESS=
